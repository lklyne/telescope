{"id":"22qkf5yt","parent_id":"awuhzpwz","name":"Slice 3: resize geometry adapter","description":"Route resize gestures through an adapter that translates the handle-drag delta into 'dragged ref points' for the alignment detector — only the moving edge(s) contribute references; the opposite (anchored) edge does not spuriously trigger guides. Consume the same snapCandidateSnapshot captured at resize-begin and feed alignmentGuideDetector. Reuse the canvas-guides broadcast pattern (no new IPC vocabulary). The resize IPC just gains the same guide-broadcast pattern as drag.","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-13T23:16:07.468Z","updated_at":"2026-05-13T23:16:07.468Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":[]}
{"id":"7kjdgp84","parent_id":"awuhzpwz","name":"Slice 1: shiftKey IPC + axisLockProjector","description":"Add a shiftKey: boolean field to the canvas-drag-entity IPC update payload. The renderer reads event.shiftKey and forwards it; main owns the projection. Implement axisLockProjector as a pure module: (rawDelta, cursorOffsetFromOrigin, shiftKey) -> constrainedDelta. Wire it into applyDragDelta to run BEFORE snapToGrid; locked axis bypasses grid-snap so origin coordinate is preserved exactly. Support mid-drag press/release and H<->V flip as cursor crosses 45 degrees. No UI changes. Pure module tested in tests/unit/axis-lock-projector.test.ts covering 8 octants, the 45-degree tie boundary, no-shift passthrough, and the multi-select implication (same delta returned regardless of count).","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-13T23:15:54.179Z","updated_at":"2026-05-13T23:15:54.179Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":[]}
{"id":"abpe8y82","parent_id":"awuhzpwz","name":"Slice 4: distribution guides + == rendering","description":"Implement distributionGuideDetector (draggedRect, candidates, axis, tolerance) -> DistributionGuide[]: detect when the dragged rect's gap to one candidate equals its gap to another (along the axis) within tolerance. Emit chains with the gap value and the involved rect endpoints. Multi-candidate chains (four entities forming three equal gaps) supported. Extend canvas-guides broadcast payload to carry distribution guides alongside alignment guides. Extend GuideOverlayLayer to render '==' measure marks for distribution guides. Pure module tested in tests/unit/distribution-guide-detector.test.ts covering equal-gap triples, near-miss tolerance boundary, multi-candidate chains, mixed-axis (horizontal distribution doesn't fire on vertical-only patterns). Also add the smoke test tests/smoke/drag-affordances.test.ts that exercises the integrated path via AppClient: place two stickies, drag one with shift held, assert canvasX (or Y) is unchanged and the moved axis grid-snaps; place two stickies vertically aligned, drag one near alignment, assert a canvas-guides broadcast emits the expected guide structure; commit and assert the guide list clears.","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-13T23:16:14.246Z","updated_at":"2026-05-13T23:16:14.246Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":[]}
{"id":"awuhzpwz","parent_id":null,"name":"Canvas drag affordances — axis lock + alignment & distribution guides","description":"# Canvas drag affordances — axis lock + alignment & distribution guides\n\nSource: https://github.com/lklyne/specular/issues/91\n\n> *This was generated by AI during planning (grill-with-docs → to-prd).*\n\n## Problem Statement\n\nDesigners and developers using Specular's canvas don't have the basic spatial-arrangement affordances they expect from FigJam and Figma. Specifically:\n\n1. There's no way to drag an entity while holding `Shift` to constrain its movement to a horizontal or vertical axis. Aligning entities by eye while preserving the other axis is tedious — users have to drag, undo, and retry, or use the inspector to manually zero one coordinate.\n2. There are no visual snapping guides that confirm when a dragged or resized entity aligns with another entity's edge or center. Users have to zoom in to read coordinates or eyeball it, then second-guess the result.\n\nThe 20 px grid-snap helps with rough placement but doesn't tell the user *when* their entity is aligned with a neighbor, doesn't help with cross-axis center-alignment between two stickies, and doesn't survive any entity placed off-grid.\n\n## Solution\n\nAdd two FigJam-parity drag affordances. Both terms are defined under `Drag affordances` in `CONTEXT.md`; the architectural trade-off is captured in [ADR 0012](../../docs/adr/0012-alignment-guides-are-visual-only.md).\n\n1. **Axis lock.** Holding `Shift` during an entity drag (with or without `Alt`/`Option` for copy) constrains the movement to a single axis. The lock is **live** — the dominant axis is recomputed every frame from the cursor's offset to the drag origin, so the user can press, release, or flip H↔V mid-drag without releasing the mouse. The locked axis bypasses grid-snap so the entity holds its exact origin coordinate.\n\n2. **Alignment & distribution guides.** During an entity drag or resize, Specular paints 1 px solid lines in the canvas accent color whenever the dragged (or resized) entity's edge or center coincides with another entity's edge or center within 0.5 px (post-grid-snap). Each candidate contributes 6 edges (top / bottom / left / right / horizontal-center / vertical-center); the dragged entity contributes its own 6 reference points (only the moving edge for resize). When the dragged entity sits at equal distance from two or more candidates along an axis, `==` measure marks render as **distribution guides**. The candidate set is a snapshot taken at gesture-begin of all entities whose rect intersects the canvas viewport, excluding the active selection. Pages and groups (snap to bbox) are included.\n\nGuides are **visual-only**: grid-snap remains the only magnetic pull, and guides only render when the post-snap position is genuinely aligned. They confirm alignments — they never create them. See ADR 0012 for the rationale and pivot path.\n\n## User Stories\n\n1. As a Specular user, I want to hold `Shift` while dragging an entity so that the entity is constrained to horizontal or vertical movement only, so that I can move things without disturbing the other axis.\n2. As a Specular user, I want the axis lock to choose the dominant axis from my cursor's offset to the drag origin, so that the constraint follows my intent rather than the first jitter of my hand.\n3. As a Specular user, I want to press and release `Shift` mid-drag, so that I can engage and disengage axis lock without releasing the mouse.\n4. As a Specular user, I want to flip H↔V mid-drag by arcing my cursor past 45° while `Shift` is held, so that I can correct mistakes without restarting the gesture.\n5. As a Specular user, I want axis lock to bypass grid-snap on the locked axis, so that an entity returns to exactly the coordinate it started at when I release the mouse on that axis.\n6. As a Specular user, I want axis lock to still grid-snap on the free axis, so that the placement feel stays consistent with normal drag.\n7. As a Specular user dragging a multi-selection, I want the constrained delta applied uniformly to every selected entity, so that the group moves as a rigid unit along one axis.\n8. As a Specular user, I want `Shift` + `Alt`/`Option` to compose, so that I can axis-lock a copy-drag while duplicating.\n9. As a Specular user, I want alignment guides to appear when my dragged entity's top, bottom, left, right, horizontal-center, or vertical-center coincides with another entity's matching reference, so that I get instant visual confirmation of alignment.\n10. As a Specular user, I want guides to only appear when the alignment is genuinely true (within 0.5 px after grid-snap), so that I can trust what I see and not be misled by guides that lie.\n11. As a Specular user, I want the guide line to span only the rects involved (from min-edge to max-edge of the candidate-and-dragged pair on the snapping axis), so that the screen isn't cluttered with full-viewport crosshairs.\n12. As a Specular user, I want guides to disappear the instant alignment is no longer true, so that I have a clean signal of \"you're aligned now\" / \"you're not.\"\n13. As a Specular user, I want guides to consider only viewport-visible entities, so that the system isn't spending budget on entities I can't see and the guide list stays focused on what's relevant on my screen.\n14. As a Specular user, I want pages (live web items) treated as snap candidates like any other entity, so that I can align stickies and shapes to a page's edges or center.\n15. As a Specular user, I want groups treated as snap candidates by their outer bounding rect (not by every child individually), so that group alignment behaves intuitively.\n16. As a Specular user, I want the active selection excluded from snap candidates, so that a dragged entity never tries to align with itself.\n17. As a Specular user, I want the snap candidate set frozen at the moment I begin the drag (pan is locked during drag anyway), so that the guide behavior is stable and predictable for the duration of the gesture.\n18. As a Specular user, I want `==` distribution guides to appear when my dragged entity sits at equal distance from two or more other entities along an axis, so that I can build evenly-spaced rows and columns visually.\n19. As a Specular user, I want alignment and distribution guides to fire during resize as well as drag, so that I can stretch an entity precisely to match another's edge or center.\n20. As a Specular user resizing an entity, I want only the moving edge to contribute dragged reference points, so that the opposite (anchored) edge doesn't spuriously trigger guides.\n21. As a Specular user, I want guides to render above entity bodies and selection chrome, so that they aren't occluded by what I'm dragging.\n22. As a Specular user, I want axis lock and guides to compose with `Alt`/`Option` copy-drag, so that copies snap and constrain too.\n23. As a Specular user, I want the grid-snap behavior I'm used to (20 px) to remain unchanged, so that nothing about my existing placement habits regresses.\n24. As an agent / CLI / smoke test driving the canvas, I want axis lock and guide detection to be expressible through the same `applyDragDelta` path that interactive drags use, so that automation produces identical results to a human drag.\n25. As a Specular maintainer, I want the axis-lock projection, snap candidate snapshot, alignment detection, and distribution detection to be pure functions tested in isolation, so that regressions in geometry are caught fast and without spinning up Electron.\n\n## Implementation Decisions\n\n**Deep, isolated modules (the four pure ones tested in `tests/unit/`):**\n\n- `axisLockProjector` — `(rawDelta, cursorOffsetFromOrigin, shiftKey) → constrainedDelta`. Picks dominant axis from cursor offset and zeros the smaller component. No-op when `shiftKey === false`. Pure; no Electron.\n- `snapCandidateSnapshot` — `(workspaceState, viewportRect, excludedIds) → SnapCandidate[]`. Returns each viewport-visible entity as `{ id, kind, top, bottom, left, right, hCenter, vCenter }`. Pages included; groups represented as their bbox (children inside an included group do not double-contribute); selection excluded. Captured once at gesture-begin.\n- `alignmentGuideDetector` — `(draggedRects, candidates, tolerance) → AlignmentGuide[]`. For each axis, finds dragged-ref ↔ candidate-edge pairs within 0.5 px. Emits guides carrying axis, target coordinate, the rect-span endpoints needed to paint the line, and the candidate entity id.\n- `distributionGuideDetector` — `(draggedRect, candidates, axis, tolerance) → DistributionGuide[]`. Detects when the dragged rect's gap to one candidate equals its gap to another (along the axis) within tolerance. Emits chains with the gap value and the involved rect endpoints.\n\n**Pipeline seam (modified):**\n\n- The existing `applyDragDelta` (the one site in main that turns a raw drag delta into entity position changes) gains two stages: axis-lock projection runs *before* `snapToGrid`; alignment + distribution detection runs *after*. Single seam where the four pure modules meet; nothing else in the runtime needs to know about guides.\n\n**Resize adapter (new):**\n\n- Resize gestures are routed through an adapter that translates the handle-drag delta into \"dragged ref points\" — only the moving edge(s) contribute references. The adapter consumes the same `snapCandidateSnapshot` captured at resize-begin and feeds the alignment detector. No new IPC vocabulary; the existing resize IPC just gains the same guide-broadcast pattern.\n\n**IPC payload changes:**\n\n- `canvas-drag-entity` update payload gains a `shiftKey: boolean` field. The renderer reads `event.shiftKey` and forwards; main owns projection.\n- A new `canvas-guides` broadcast from main carries the per-update guide list (alignment + distribution). Sent on every drag/resize update; empty list clears the overlay. Sent on commit/cancel to clear.\n\n**Renderer (new layer in aboveView):**\n\n- `GuideOverlayLayer` is a sibling of the existing `DragCopyPreviewLayer` in `aboveView`. It subscribes to `canvas-guides` and paints 1 px solid lines for alignment guides (spanning the involved rects) and `==` marks for distribution guides. Pure presentation — no business logic, no state of its own beyond the subscription.\n\n**Architectural respect:**\n\n- All projection and detection happens in main (`src/main/runtime/`), per the interaction-layer invariant that main owns workspace state and renderers are clients. No second projection lives in the renderer (rejected during grilling to keep the snap engine consolidated).\n- No new `InteractionMode` is introduced. Axis lock and guides are payload features of the existing `dragging-entities` and `resizing-entity` modes, per the spec's \"5.6 Should I add a new interaction mode?\" heuristic.\n\n**Out of scope deviations from FigJam:**\n\n- Center alignment will rarely engage because the post-grid-snap position is unlikely to satisfy the 0.5 px equality on centers (an entity's center sits at `x + width / 2`; off-grid unless width is a multiple of 40). This is the explicit trade-off captured in ADR 0012 — the cost of guides being honest given the 20 px grid stays.\n\n## Testing Decisions\n\n**Good-test bar:** Each test asserts an external behavior (function input → output), not internals. A test \"passes its bar\" if it would have caught a real regression (you can name the production-code mutation that breaks it), survives a refactor, and is locally legible from the test file alone. This matches the project standard in `tests/README.md` (per issue #81).\n\n**Unit tests (all four pure modules), in `tests/unit/`:**\n\n- `tests/unit/axis-lock-projector.test.ts` — dominant-axis selection across the 8 octants of cursor offset; mid-drag flip behavior as cursor crosses 45°; no-shift passthrough; ties-at-exact-45° resolved deterministically; multi-select implication (same delta returned regardless of count, since callers apply it uniformly).\n- `tests/unit/snap-candidate-snapshot.test.ts` — viewport intersection (entity fully inside, partially clipping, fully outside); selection exclusion; page kind included with its full bbox; group kind contributes its bbox once, children inside the group don't double-contribute; empty workspace → empty snapshot.\n- `tests/unit/alignment-guide-detector.test.ts` — 6×6 cross of dragged-ref × candidate-edge pairs across both axes; 0.5 px tolerance boundary (inside and outside); no-match returns empty; multi-candidate match (nearest wins per axis); span endpoints span both rects correctly.\n- `tests/unit/distribution-guide-detector.test.ts` — equal-gap triples; near-miss at tolerance boundary; multi-candidate chains (four entities forming three equal gaps); mixed axis (horizontal distribution doesn't fire on vertical-only patterns).\n\n**Smoke test (one), in `tests/smoke/`:**\n\n- `tests/smoke/drag-affordances.test.ts` — exercises the integrated path end-to-end via the existing `AppClient` HTTP API: place two stickies, drag one with `Shift` held, assert the entity's `canvasX` (or `Y`) is unchanged and the moved axis grid-snaps; place two stickies vertically aligned, drag one near alignment, assert a `canvas-guides` broadcast emits the expected guide structure; commit and assert the guide list clears.\n\n**Prior art:**\n\n- Unit-test style for pure runtime helpers: `tests/unit/zoom.test.ts` (behavior-driven blocks, per the post-issue-#81 standard), `tests/unit/interaction-controller.test.ts` (public-API-driven, observable outputs).\n- Smoke harness: `tests/smoke/test-utils.ts` `AppClient`; comparable file shape `tests/smoke/gestures.test.ts`.\n- IPC payload extension reference: how `canvas-drag-copy-selection` flows through `register-canvas-drag-ipc.ts` is the closest analog for the `shiftKey` payload addition and the `canvas-guides` broadcast.\n\n## Out of Scope\n\n- **Magnetic alignment guides.** Guides are visual-only in v1 (ADR 0012). Making them pull is a separate, larger product decision that probably implies dropping or shrinking grid-snap.\n- **Canvas-center guide.** FigJam shows a center-of-canvas alignment line for the page itself; deliberately deferred.\n- **Suppressing-snap modifier.** No second modifier (e.g. `Cmd`/`Ctrl`) to disable grid-snap or guides; `Shift` is claimed for axis lock.\n- **Smart spacing manipulation.** Distribution guides are detection-only — they don't add the FigJam interaction where you drag a distribution gap to redistribute.\n- **Edge-drag alignment.** Connection-edge drags don't snap to entity edges.\n- **Marquee alignment.** Marquee gestures don't trigger guides.\n- **Cross-canvas alignment.** Snap candidates come from the current canvas only.\n- **Configurable tolerance / colors.** Both are constants in v1; no user-facing setting.\n- **Performance regression coverage.** The snapshot-at-begin design is cheap by construction; no perf budgets are encoded in tests.\n\n## Further Notes\n\n- The four-slice tracer-bullet decomposition for `/to-issues` is expected to be: (1) `shiftKey` flag on `canvas-drag-entity` IPC + `axisLockProjector` wired into `applyDragDelta`, no UI yet; (2) `snapCandidateSnapshot` + `alignmentGuideDetector` + `canvas-guides` broadcast + `GuideOverlayLayer`; (3) resize geometry adapter; (4) `distributionGuideDetector` + `==` rendering. Each independently mergeable.\n- `CONTEXT.md` was updated in the same PR that this PRD references (PR #89) with a new `Drag affordances` section defining `Axis lock`, `Snap candidate`, `Alignment guide`, and `Distribution guide`. ADR 0012 was also added there.\n- The `editing-entity` and `dragging-edge` modes are unaffected; guide and axis-lock logic only attaches to `dragging-entities` and `resizing-entity`.\n- The smoke test deliberately exercises the broadcast pattern rather than the rendered overlay — overlay rendering is presentation-only and not where regressions will live.\n","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-13T23:15:46.887Z","updated_at":"2026-05-13T23:15:46.887Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":["7kjdgp84","br8o9jr6","22qkf5yt","abpe8y82"]}
{"id":"br8o9jr6","parent_id":"awuhzpwz","name":"Slice 2: alignment guides + GuideOverlayLayer","description":"Implement snapCandidateSnapshot (workspaceState, viewportRect, excludedIds) -> SnapCandidate[]: each viewport-visible entity contributes {id, kind, top, bottom, left, right, hCenter, vCenter}. Pages included; groups represented by their bbox once (children inside an included group don't double-contribute); selection excluded. Captured once at gesture-begin. Implement alignmentGuideDetector (draggedRects, candidates, tolerance) -> AlignmentGuide[]: for each axis, find dragged-ref <-> candidate-edge pairs within 0.5 px tolerance post-grid-snap. Emit guides carrying axis, target coordinate, the rect-span endpoints needed to paint the line (from min-edge to max-edge of the candidate+dragged pair), and the candidate entity id. Wire into applyDragDelta to run AFTER snapToGrid. Add canvas-guides broadcast from main carrying the per-update guide list; sent on every drag update and on commit/cancel (empty list clears). Add GuideOverlayLayer in aboveView as a sibling of DragCopyPreviewLayer — subscribes to canvas-guides and paints 1 px solid lines in the canvas accent color. Pure modules tested in tests/unit/snap-candidate-snapshot.test.ts and tests/unit/alignment-guide-detector.test.ts.","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-13T23:16:02.813Z","updated_at":"2026-05-13T23:16:02.813Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":[]}
{"id":"fca2e239","parent_id":"ypz5rro1","name":"C — delete renderer keydown hooks; port to registry","description":"Delete renderer keydown listeners; route renderer-targeted bindings via 'binding-fire' IPC.\n\nFiles deleted:\n- src/renderer/shared/hooks/useAnnotateToggleShortcut.ts\n- src/renderer/shared/hooks/useAnnotationOverlayShortcuts.ts\n- v/c/d/escape/delete branches removed from src/renderer/canvas-bg/useCanvasGlobalShortcuts.ts (becomes empty for keyboard; remove the hook entirely after clipboard is split out)\n\nFiles added/changed:\n- src/renderer/canvas-bg/useCanvasClipboard.ts — clipboard event handling extracted from useCanvasGlobalShortcuts.ts. Owns Cmd+C / Cmd+V / Cmd+X via DOM ClipboardEvents (NOT the binding registry — see plan §Clipboard split). Hijack rule: native wins if target is a typing target or window.getSelection().toString() is non-empty.\n- src/renderer/above-view/binding-handlers.ts — Record<AboveViewBindingId, () => void> with annotation-close-thread and annotation-clear-draft handlers (renderer-local React useState for openThreadId / pendingAnnotation).\n- src/renderer/shared/hooks/useRendererBindingHandlers.ts — generic hook that listens for 'binding-fire' IPC and invokes the matching handler from a passed-in map.\n- Renderer App.tsx files (above-view) updated to call useRendererBindingHandlers(aboveViewBindingHandlers).\n\nPaste behavior in useCanvasClipboard.ts calls pasteFromClipboard() in main with the existing smart-resolution order (entity JSON → URL → image → plain text → no-op). The plain text → sticky note path and looksLikeUrl() page creation may already exist; verify or add.\n\nActive page intercepts nothing — when page focus is held, the page receives clipboard events natively. No listeners on the page.\n\nTool-toggle behavior removed: pressing 'c' while in comment tool no longer goes back to select. Escape is the only way back to select. Changelog entry required.\n\nSmoke tests added in Step B should continue to pass. Verify v / c / m / Escape work from aboveView focus (where the deleted hooks used to handle them).\n\nReferences:\n- docs/plans/keyboard-binding-registry.md §Renderer handler maps, §Step C, §Clipboard split, §No tool-toggle on second keypress\n- docs/adr/0010-main-as-sole-shortcut-dispatch-site.md","priority":1,"completed":true,"result":"Merged PR #78. Renderer keydown hooks deleted; bindings now route through 'binding-fire' IPC. Clipboard split into useCanvasClipboard.","metadata":{"commit":{"sha":"7a0cee08d40a8d84c2bd437c6e22c18cc7288042","message":"feat(keyboard): Step C — delete renderer keydown hooks; port to registry (#78)","branch":"claude/feat-keyboard-binding-registry","timestamp":"2026-05-13T01:24:22.113Z"}},"created_at":"2026-05-12T21:27:12.149Z","updated_at":"2026-05-13T01:24:22.114Z","started_at":"2026-05-13T00:26:00.000Z","completed_at":"2026-05-13T01:24:22.114Z","blockedBy":["y46q42dn"],"blocks":["y41x08m4"],"children":[]}
{"id":"j4p66k15","parent_id":null,"name":"Keyboard Binding Registry","description":"# Keyboard Binding Registry\n\nBranch: `claude/explore-keyboard-system-N5fjt`. Plan for collapsing Specular's\nkeyboard system into a single binding registry. The registry is the source of\ntruth for every shortcut; main is the sole dispatch site; renderer keydown\nlisteners go away. See [ADR 0010](../adr/0010-main-as-sole-shortcut-dispatch-site.md)\nand [ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md).\n\n## Status\n\n| Step | Status |\n|---|---|\n| A — registry + dispatch + tests, no callers | ⬜ not started |\n| B — port main's `keyboard-shortcuts.ts` to the registry | ⬜ not started |\n| C — delete renderer keydown hooks; port to registry | ⬜ not started |\n| D — `app-menu.ts` accelerators read from the registry | ⬜ not started |\n| E — adjacent: `Cmd+A` select-all, Cmd+D no-grouping, paste-smart, P2 page focus | ⬜ not started |\n\nEach step ends in a working app. Each ships as one PR.\n\n## Context\n\nKeyboard shortcuts today are scattered across:\n\n- `src/main/runtime/keyboard-shortcuts.ts` — 285 lines of `if (input.type === 'keyDown' && input.meta && …)` blocks.\n- Three renderer hooks (`useCanvasGlobalShortcuts`, `useAnnotateToggleShortcut`, `useAnnotationOverlayShortcuts`) that re-handle the same v/c/d keys when focus is in non-canvas views.\n- `src/main/runtime/app-menu.ts` accelerator strings, totally disconnected from the rest.\n- Per-WebContents `handleShortcuts: false` flag, which is the implicit \"this view doesn't fire tool shortcuts\" filter.\n\nEffects:\n\n- \"Where is the `v` shortcut defined?\" has three answers depending on which WebContents has focus.\n- No tests exist on any of the keyboard code.\n- The wiring uses a late-bound setter pattern (`wireKeyboardShortcuts(...)`) to dodge an import cycle.\n- ADR 0005 explicitly flagged keyboard shortcuts as the natural follow-up to the unified `Tool` concept. ADR 0009 §93 flagged variant-key bindings (R/E/D, etc.) as a follow-up to tool-defaults.\n\nThis plan finishes that follow-up.\n\n## Goals\n\n1. **One source of truth for every shortcut.** The Bindings UI and the menu accelerators and the dispatcher all read the same table.\n2. **One dispatch site.** Main's `before-input-event`. No renderer `keydown` listeners for shortcuts. ([ADR 0010](../adr/0010-main-as-sole-shortcut-dispatch-site.md))\n3. **Coverage parity.** Every `Tool['kind']` has a default key (TypeScript exhaustiveness enforces it). Adding a new tool without a key is a build error.\n4. **Predictable suppression.** Plain letters never fire while typing. Modifier shortcuts opt in to firing while typing. Same shape applies to page focus. ([ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md))\n5. **Pure-function test surface.** Dispatch is unit-testable without spawning Electron.\n\n## Non-goals (v1)\n\n- Bindings settings pane.\n- User overrides (no `userData/bindings.json`).\n- `?` cheatsheet overlay.\n- Toolbar tooltips. (Tooltips land in a later PR and become the source of truth for discoverability.)\n\n## Architecture\n\n### Modules\n\n```\nsrc/shared/bindings.ts            // pure data + types + dispatchKey()\nsrc/main/runtime/binding-handlers.ts   // Record<BindingId, (ctx) => void>\nsrc/main/runtime/binding-dispatcher.ts // watchModifierKeys replacement; per-WebContents adapter\nsrc/renderer/{view}/binding-handlers.ts // small per-renderer handler maps (only for renderer-targeted bindings)\nsrc/renderer/{view}/useRendererBindingHandlers.ts // tiny hook that listens for 'binding-fire' IPC\n```\n\nDeleted:\n- `keyboard-shortcuts.ts` (replaced by `binding-dispatcher.ts` + `binding-handlers.ts`)\n- `useAnnotateToggleShortcut.ts`\n- `useAnnotationOverlayShortcuts.ts`\n- v/c/d/escape/delete branches of `useCanvasGlobalShortcuts.ts` (clipboard remains, split into `useCanvasClipboard.ts`)\n- `wireKeyboardShortcuts(...)` late-bound setter pattern\n- `handleShortcuts: false` per-view flag\n\n### Types\n\n```ts\n// src/shared/bindings.ts\n\nexport type KeyboardSourceView =\n  | 'aboveView' | 'canvasBg'\n  | 'toolbar' | 'leftSidebar' | 'rightDetailsPanel'\n  | 'devtoolsHeader' | 'devtoolsResizeHandle'\n  | 'page'\n\nexport type NormalizedKey = {\n  key: string      // lowercased, e.g. 'v', 'escape', 'arrowleft'\n  cmd: boolean     // CmdOrCtrl\n  alt: boolean\n  shift: boolean\n}\n\nexport type BindingId =\n  | 'tool-select' | 'tool-add-page' | 'tool-add-text-plain' | 'tool-add-text-sticky'\n  | 'tool-add-shape-rectangle' | 'tool-add-shape-ellipse' | 'tool-add-shape-diamond'\n  | 'tool-comment' | 'tool-draw-pen' | 'tool-draw-highlight' | 'tool-inspect'\n  | 'undo' | 'redo' | 'reset-viewport' | 'group' | 'ungroup'\n  | 'select-all' | 'duplicate' | 'delete-selection'\n  | 'nav-left' | 'nav-right' | 'nav-up' | 'nav-down'\n  | 'escape-tool' | 'escape-page-focus'\n  | 'close-tab'\n  | 'annotation-close-thread' | 'annotation-clear-draft'\n\nexport type BindingTarget = 'main' | KeyboardSourceView\n\nexport type BindingContext = {\n  activeTool: Tool\n  isTextEditing: boolean\n  arrowNavigationLocked: boolean\n  hasKeyboardTargetPage: boolean\n  pageFocusActive: boolean\n  canUndo: boolean\n  canRedo: boolean\n  selectionEmpty: boolean\n  sourceView: KeyboardSourceView\n  viewMode: 'canvas' | 'browser'\n}\n\nexport type Binding = {\n  id: BindingId\n  defaultKey: NormalizedKey\n  scope: KeyboardSourceView[]        // which source views can fire this binding\n  target: BindingTarget              // where the handler runs\n  firesWhileTyping?: boolean         // default false\n  firesFromPageFocus?: boolean       // default false\n  when?: (ctx: BindingContext) => boolean\n  label: string                      // for future Bindings pane + tooltips\n}\n```\n\n### Dispatch\n\n```ts\n// src/shared/bindings.ts\n\nexport function dispatchKey(\n  table: readonly Binding[],\n  key: NormalizedKey,\n  ctx: BindingContext,\n): BindingId | null\n```\n\nPure function. Returns the first binding whose `defaultKey` matches the input\nkey and whose `scope`/`when`/`firesWhileTyping`/`firesFromPageFocus` predicates\nall hold.\n\nMatch order: table order. No conflict detection needed because each binding's\nscope is explicit; if two bindings have the same key + scope + modifiers, that's\na typo, caught by a unit test that asserts uniqueness.\n\n### Main adapter\n\n```ts\n// src/main/runtime/binding-dispatcher.ts\n\nexport function attachBindingDispatcher(\n  webContents: WebContents,\n  sourceView: KeyboardSourceView,\n): void\n```\n\nRegisters a `before-input-event` listener. On keydown:\n\n1. Normalize the Electron `Input` → `NormalizedKey`.\n2. Build `BindingContext` from runtime state (active tool, text-editing flag, …).\n3. Call `dispatchKey(BINDINGS, key, { ...ctx, sourceView })`.\n4. If a `BindingId` came back:\n   - `event.preventDefault()`.\n   - If `binding.target === 'main'`: invoke `mainHandlers[id](ctx)`.\n   - Else: `viewFor(binding.target).webContents.send('binding-fire', id)`.\n\nThis replaces `watchModifierKeys`. The `sourceView` argument replaces the\n`handleShortcuts: false` flag.\n\n### Main handler map\n\n```ts\n// src/main/runtime/binding-handlers.ts\n\nexport const mainHandlers: Record<MainBindingId, (ctx: BindingContext) => void>\n```\n\nImports concrete runtime functions directly. No late-bound setters. The cycle\nthat motivated `wireKeyboardShortcuts(...)` is broken by placing this file at\na layer that runs *after* `runtime-core` is initialized.\n\n### Renderer handler maps\n\n```ts\n// src/renderer/above-view/binding-handlers.ts\n\nexport const aboveViewBindingHandlers: Record<AboveViewBindingId, () => void>\n```\n\nRegistered via a small hook on mount:\n\n```ts\nuseRendererBindingHandlers(aboveViewBindingHandlers)\n```\n\nThe hook listens for the `binding-fire` IPC and invokes the matching handler.\nTwo bindings use this path today: `annotation-close-thread` and\n`annotation-clear-draft`, because their state (`openThreadId`,\n`pendingAnnotation`) is renderer-local React `useState`.\n\n### Menu accelerators\n\n```ts\n// src/main/runtime/app-menu.ts\n\nimport { acceleratorFor } from './binding-accelerator'\n\n{ label: 'Close Tab', accelerator: acceleratorFor('close-tab'), click: () => mainHandlers['close-tab']({}) }\n```\n\n`acceleratorFor(id)` returns the `CmdOrCtrl+W`-style string derived from\n`bindingById(id).defaultKey`. The menu and the dispatcher cannot drift.\n\nMenu items without a binding (About dialog, Setup) keep their inline `click:`\n— they're not bindable anyway.\n\n## The binding table\n\n| Id | Default key | Scope | Target | While typing? | From page focus? |\n|---|---|---|---|---|---|\n| `tool-select` | `v` | canvas-region¹ | main | — | — |\n| `tool-add-page` | `p` | canvas-region | main | — | — |\n| `tool-add-text-plain` | `t` | canvas-region | main | — | — |\n| `tool-add-text-sticky` | `s` | canvas-region | main | — | — |\n| `tool-add-shape-rectangle` | `r` | canvas-region | main | — | — |\n| `tool-add-shape-ellipse` | `o` | canvas-region | main | — | — |\n| `tool-add-shape-diamond` | `Shift+R` | canvas-region | main | — | — |\n| `tool-comment` | `c` | canvas-region | main | — | — |\n| `tool-draw-pen` | `m` | canvas-region | main | — | — |\n| `tool-draw-highlight` | `Shift+M` | canvas-region | main | — | — |\n| `tool-inspect` | `i` | canvas-region | main | — | — |\n| `escape-tool` | `Escape` | all | main | yes (commits text edit instead) | yes |\n| `escape-page-focus` | `Escape` | `page` | main | — | yes |\n| `undo` | `Cmd+Z` | all | main | **yes** | — |\n| `redo` | `Cmd+Shift+Z` | all | main | **yes** | — |\n| `reset-viewport` | `Cmd+1` | all | main | **yes** | **yes** |\n| `group` | `Cmd+G` | canvas-region | main | — | — |\n| `ungroup` | `Cmd+Shift+G` | canvas-region | main | — | — |\n| `select-all` | `Cmd+A` | canvas-region | main | — | — |\n| `duplicate` | `Cmd+D` | canvas-region | main | — | — |\n| `delete-selection` | `Delete` / `Backspace` | canvas-region | main | — | — |\n| `nav-left` | `ArrowLeft` | canvas-region | main | — | — |\n| `nav-right` | `ArrowRight` | canvas-region | main | — | — |\n| `nav-up` | `ArrowUp` | canvas-region | main | — | — |\n| `nav-down` | `ArrowDown` | canvas-region | main | — | — |\n| `close-tab` | `Cmd+W` | all | main | yes | — |\n| `annotation-close-thread` | `Escape` | `aboveView` | `aboveView` | — | — |\n| `annotation-clear-draft` | `Escape` | `aboveView` | `aboveView` | — | — |\n\n¹ `canvas-region` is a convenience for `['aboveView', 'canvasBg', 'toolbar', 'rightDetailsPanel']` — the four views where canvas-style shortcuts should fire. Constant defined in `src/shared/bindings.ts`.\n\n### `add-document` intentionally unbound\n\nHas no default key. Users add documents via the toolbar's \"Add text ▾\"\ndropdown. Bindable later via the (future) Bindings pane.\n\n### `Escape` resolution order\n\nThree bindings share `Escape`. Match order is:\n\n1. `annotation-close-thread` / `annotation-clear-draft` (scope: `aboveView`, only when active state predicate holds — the `when` checks for `openThreadId` / `pendingAnnotation`).\n2. `escape-page-focus` (scope: `page` only, only when page focus is active).\n3. `escape-tool` (all scopes, fires whenever `activeTool.kind !== 'select'`).\n\nEach binding's `when` predicate ensures only one fires per keystroke; if none of\nthem match, Escape falls through to native handling.\n\n### Variant key handlers\n\nPer [ADR 0009](../adr/0009-tool-variants-in-popup-state.md), shape and brush\nvariants live in tool defaults. The keyboard handler does **both**:\n\n```ts\n'tool-add-shape-rectangle': () => {\n  setActiveTool({ kind: 'add-shape' })\n  setToolDefault('add-shape.shapeKind', 'rectangle')\n}\n'tool-draw-highlight': () => {\n  setActiveTool({ kind: 'draw' })\n  setToolDefault('draw.brushType', 'highlight')\n}\n```\n\nNo compound-action framework needed — it's just a multi-statement handler body.\n\n## Behavior rules\n\n### No tool-toggle on second keypress\n\nFigJam reference: pressing the same tool key twice does nothing (the tool stays\nactive). Escape is the only way back to `select`. This removes the existing\ntoggle behavior in `useAnnotateToggleShortcut.ts`.\n\n### Suppression while typing\n\nDefault: bindings do not fire while `isTextEditing` is true. Opt-in via\n`firesWhileTyping: true`. Four bindings opt in:\n\n- `undo`, `redo` — global Yjs undo spans text + canvas edits.\n- `reset-viewport` — hand-of-god. Viewport reset doesn't mutate the document.\n- `escape-tool` — Escape in a text input commits the edit; the text-edit\n  context check inside the handler prevents double-firing as a tool reset.\n\n`Cmd+A` is contextual: when `isTextEditing`, the native \"select all text\" wins\nbecause the binding's `firesWhileTyping` is `false`. When not typing, our\n`select-all` handler invokes `selectAllEntities()`.\n\n### Page focus (P2 policy)\n\nPer [ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md), when a\npage has keyboard focus, the page wins almost all keystrokes. Specular reserves\nonly:\n\n- `escape-page-focus` — exit page focus back to canvas.\n- `reset-viewport` — hand-of-god.\n\nCmd+Z, Cmd+G, Cmd+W, arrows, and tool keys all fall through to the page\nnatively. To use canvas undo while page-focused, the user presses Escape first.\n\nThis is a behavior change from today. Changelog entry required.\n\n## Clipboard split\n\nClipboard handling is **not** in the registry. `Cmd+C` / `Cmd+V` / `Cmd+X` arrive\nas DOM `ClipboardEvent`s, not as keystrokes — and `event.clipboardData` /\n`window.getSelection()` only exist renderer-side.\n\nThe new `src/renderer/canvas-bg/useCanvasClipboard.ts` (extracted from\n`useCanvasGlobalShortcuts.ts`) owns this. The hijack rule:\n\n- If `event.target` is a typing target, native wins.\n- If `window.getSelection().toString()` is non-empty, native wins.\n- Else, the canvas hijacks.\n\nOn hijack:\n\n- **Copy** with entity selection → entity JSON to clipboard.\n- **Cut** with entity selection → copy + delete.\n- **Paste** smart-resolution order (`pasteFromClipboard()` in main):\n  1. Our entity-shaped JSON → paste entities.\n  2. Text that `looksLikeUrl()` matches → create a page (reuses existing URL paster).\n  3. Image data → create a file entity.\n  4. Plain text → create a sticky note.\n  5. Otherwise → no-op.\n\nActive page intercepts nothing — when page focus is held, the page receives\nclipboard events natively. The renderer hook installs no listeners on the page.\n\n## Duplicate cleanup\n\n`Cmd+D` reuses `duplicatePageFromSource` / `duplicateEntity`. Side cleanup:\n\n- Default `skipGrouping: true` across all three call sites in\n  `register-canvas-entity-ipc.ts` and `register-right-details-panel-ipc.ts`.\n- Remove the `mode: 'duplicate'` grouping codepath through `addPageFromSource`.\n\nThis is a behavior change for the existing context-menu and right-details-panel\nduplicate buttons — they no longer auto-create row groups. Changelog entry\nrequired.\n\n## Build order\n\n### Step A — registry + dispatch + tests (no callers)\n\nFiles added:\n- `src/shared/bindings.ts` — types, the table, `dispatchKey()`, `normalizeElectronInput()`, `acceleratorString()`.\n- `tests/unit/bindings.test.ts` — table uniqueness, dispatch for every binding × source view × modifier combination, scope and predicate filtering.\n\nFiles unchanged. App behavior unchanged. Pure addition. Zero risk.\n\n### Step B — port main\n\nFiles added/changed:\n- `src/main/runtime/binding-handlers.ts` — main-side handler map.\n- `src/main/runtime/binding-dispatcher.ts` — `attachBindingDispatcher(wc, sourceView)`.\n- `src/main/runtime/window-init.ts` — replace `watchModifierKeys(...)` calls with `attachBindingDispatcher(...)`.\n- `src/main/runtime/page-factory.ts` — same swap.\n- `src/main/ipc/register-canvas-ipc.ts` — `setTextEditingActive` import path stays.\n\nFiles deleted:\n- `src/main/runtime/keyboard-shortcuts.ts`.\n\nThe `wireKeyboardShortcuts(...)` injection in `window-init.ts:132` is deleted;\n`binding-handlers.ts` imports its dependencies directly.\n\nAdjacent IPC additions:\n- `selectAllEntities` — new IPC handler that selects every entity in the active tab. Used by `Cmd+A` when not typing.\n\nExisting smoke tests should pass unchanged. Add:\n- `tests/smoke/keyboard-shortcuts.test.ts` — happy path for every binding.\n\n### Step C — delete renderer hooks\n\nFiles deleted:\n- `src/renderer/shared/hooks/useAnnotateToggleShortcut.ts`.\n- `src/renderer/shared/hooks/useAnnotationOverlayShortcuts.ts`.\n- v/c/d/escape/delete branches removed from `useCanvasGlobalShortcuts.ts` (it becomes empty for keyboard; remove the hook).\n\nFiles added/changed:\n- `src/renderer/canvas-bg/useCanvasClipboard.ts` — clipboard events only.\n- `src/renderer/above-view/binding-handlers.ts` — `annotation-close-thread`, `annotation-clear-draft`.\n- `src/renderer/shared/hooks/useRendererBindingHandlers.ts` — generic IPC listener.\n\nRenderer App.tsx files updated to use the new hooks.\n\n### Step D — port menu accelerators\n\nFiles changed:\n- `src/main/runtime/app-menu.ts` — `accelerator: acceleratorFor('close-tab')` etc.\n- `src/main/runtime/binding-accelerator.ts` — `acceleratorFor(id)` helper.\n\nThe menu cannot drift from the dispatcher; they read the same table.\n\n### Step E — adjacent behavior changes\n\nLands as part of Step B/C or split into its own PR. Each is small but\nuser-visible:\n\n- `select-all` action + Cmd+A wiring.\n- `duplicate` action: reuse helpers, drop auto-grouping in all paths.\n- Smart-paste resolution.\n- P2 page-focus enforcement.\n\nEach gets a changelog entry. Alt-drag-to-duplicate (pointer-router work) is\n**adjacent** to this plan and should ship in the same release for narrative\ncoherence, but is tracked separately because it touches the router not the\nkeyboard.\n\n## Tests\n\n### Unit\n\n- `bindings.test.ts` — the table is well-formed (every `Tool['kind']` has a binding; no two bindings share `(key, scope, modifiers)`).\n- `bindings.test.ts` — `dispatchKey` returns the right id for every binding × source-view × context combination.\n- `bindings.test.ts` — `acceleratorString` round-trips for every modifier combination.\n- `bindings.test.ts` — `firesWhileTyping` and `firesFromPageFocus` defaults are respected.\n\n### Smoke\n\n- `keyboard-shortcuts.test.ts` — Cmd+Z, Cmd+G, v, c, m, paste-URL-creates-page, paste-text-creates-sticky, Cmd+A select-all, Cmd+D duplicate-no-grouping, Escape exits page focus, Escape exits tool, Cmd+1 from page focus resets viewport.\n\n### Regression\n\n- Existing tests for tool switching, undo/redo, grouping, paste — should all pass unchanged after the port. Only the wiring changed, not the actions.\n\n## Out of scope\n\n- Bindings settings pane (UI).\n- User-override storage.\n- Toolbar tooltips (separate PR; reads from the same registry).\n- `?` cheatsheet overlay (later).\n- Empty-canvas onboarding hints.\n- Alt-drag-to-duplicate (pointer-router work; adjacent).\n- Promoting `pendingAnnotation` / `openThreadId` to main runtime state (would\n  let those bindings be `target: 'main'`; not worth the scope creep here).\n\n## Migration notes\n\n- `wireKeyboardShortcuts(...)` deleted — search for it; one call site in `window-init.ts`.\n- `watchModifierKeys(...)` deleted — every call site swaps to `attachBindingDispatcher(wc, sourceView)`.\n- `handleShortcuts: false` flag deleted — replaced by per-binding `scope`.\n- `setTextEditingActive(...)` stays — it's the same flag, same IPC, same logic.\n- `useAnnotateToggleShortcut` / `useAnnotationOverlayShortcuts` deleted; consumers updated.\n- Tool-toggle behavior removed: pressing `c` while in comment tool no longer goes back to select. Changelog.\n- Duplicate no longer auto-groups. Changelog.\n- Page focus no longer captures Cmd+Z and friends. Changelog.\n\n## Future work\n\n- **Bindings pane in Settings.** Reads `listBindings()`. β1 (read-only) for v1\n  of the pane; β2 (rebinding + `userData/bindings.json`) later.\n- **Toolbar tooltips.** Reads `binding.label` + `acceleratorString(binding.key)`\n  for each toolbar button. Will be the source of truth for discoverability.\n- **`?` overlay.** Modal cheatsheet listing all bindings. One-day add once the\n  registry exists.\n- **Per-binding context labels.** \"Group entities (when entities selected)\" —\n  the `when` predicate gets a human-readable explanation for the Bindings pane.\n","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-12T21:16:23.639Z","updated_at":"2026-05-12T21:16:23.639Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":[]}
{"id":"ltiqxivt","parent_id":"ypz5rro1","name":"A — registry + dispatch + tests, no callers","description":"Pure addition. Zero risk. App behavior unchanged.\n\nFiles added:\n- src/shared/bindings.ts — types, the table, dispatchKey(), normalizeElectronInput(), acceleratorString().\n- tests/unit/bindings.test.ts — table uniqueness, dispatch for every binding × source view × modifier combination, scope and predicate filtering.\n\nTypes in src/shared/bindings.ts:\n- KeyboardSourceView, NormalizedKey, BindingId, BindingTarget, BindingContext, Binding (see plan §Types)\n- Constant canvas-region = ['aboveView', 'canvasBg', 'toolbar', 'rightDetailsPanel']\n\nThe full binding table (28 entries) is in the plan under \"The binding table\". Three Escape bindings resolve in order: annotation-close-thread / annotation-clear-draft → escape-page-focus → escape-tool, with each `when` predicate ensuring only one fires per keystroke.\n\nDispatch is a pure function:\n  export function dispatchKey(table, key, ctx): BindingId | null\nReturns the first binding whose defaultKey matches, scope includes ctx.sourceView, when() (if any) holds, and firesWhileTyping/firesFromPageFocus respect their defaults (false). Match order is table order.\n\nUnit tests cover:\n- table is well-formed (every Tool['kind'] has a binding; no two bindings share (key, scope, modifiers))\n- dispatchKey returns the right id for every binding × source-view × context combination\n- acceleratorString round-trips for every modifier combination\n- firesWhileTyping and firesFromPageFocus defaults are respected\n\nFiles unchanged. No callers wired up yet — that's Step B.\n\nReferences:\n- docs/plans/keyboard-binding-registry.md §Architecture, §The binding table, §Step A\n- docs/adr/0010-main-as-sole-shortcut-dispatch-site.md\n- docs/adr/0011-page-focus-respects-native-shortcuts.md","priority":1,"completed":true,"result":"Merged PR #75, sha c3bff8744db85a307c3b4558d5660f2d5a7d7fe7","metadata":null,"created_at":"2026-05-12T21:26:47.850Z","updated_at":"2026-05-12T22:14:35.000Z","started_at":"2026-05-12T21:50:00.000Z","completed_at":"2026-05-12T22:14:35.000Z","blockedBy":[],"blocks":["y46q42dn"],"children":[]}
{"id":"r8cru8pl","parent_id":"ypz5rro1","name":"E — adjacent: Cmd+A select-all, Cmd+D no-grouping, paste-smart, P2 page focus","description":"Adjacent behavior changes. Each is small but user-visible — each gets a changelog entry.\n\n1) Cmd+A select-all\n- Wire the select-all binding (handler invokes selectAllEntities IPC added in Step B).\n- Cmd+A is contextual: when isTextEditing, the native \"select all text\" wins because firesWhileTyping is false.\n\n2) Cmd+D duplicate, no auto-grouping\n- duplicate handler reuses duplicatePageFromSource / duplicateEntity.\n- Default skipGrouping: true across all three call sites:\n  - src/main/ipc/register-canvas-entity-ipc.ts\n  - src/main/ipc/register-right-details-panel-ipc.ts\n  - the new Cmd+D handler\n- Remove the mode: 'duplicate' grouping codepath through addPageFromSource.\n- Behavior change for context-menu and right-details-panel duplicate buttons — they no longer auto-create row groups. Changelog entry required.\n\n3) Smart-paste resolution\n- pasteFromClipboard() in main resolves clipboard content in order:\n  1. Our entity-shaped JSON → paste entities\n  2. Text that looksLikeUrl() matches → create a page (reuses existing URL paster)\n  3. Image data → create a file entity\n  4. Plain text → create a sticky note\n  5. Otherwise → no-op\n- The text-→-sticky path is likely new; the URL path likely exists. Audit and add what's missing.\n\n4) P2 page-focus enforcement (per ADR 0011)\n- When a page has keyboard focus, the page wins almost all keystrokes. Specular reserves only:\n  - escape-page-focus — exits page focus back to canvas\n  - reset-viewport (Cmd+1) — hand-of-god\n- Cmd+Z, Cmd+G, Cmd+W, arrows, and tool keys all fall through to the page natively (achieved via firesFromPageFocus: false default — the dispatcher already does this from Step B; this step verifies behavior end-to-end and adds the smoke test).\n- Behavior change from today. Changelog entry required.\n\nSmoke test additions in tests/smoke/keyboard-shortcuts.test.ts:\n- Cmd+A select-all\n- Cmd+D duplicate-no-grouping\n- paste-URL-creates-page\n- paste-text-creates-sticky\n- Escape exits page focus\n- Cmd+1 from page focus resets viewport\n\nOut of scope (do not include): Alt-drag-to-duplicate (pointer-router work; ships separately). Bindings settings pane. Toolbar tooltips. ? cheatsheet overlay.\n\nReferences:\n- docs/plans/keyboard-binding-registry.md §Step E, §Duplicate cleanup, §Clipboard split, §Page focus (P2 policy)\n- docs/adr/0011-page-focus-respects-native-shortcuts.md","priority":1,"completed":true,"result":"Step E — adjacent shortcuts (Cmd+A select-all, Cmd+D no-grouping, paste-smart, P2 page focus) ported to the registry. Merged PR #80.","metadata":{"commit":{"sha":"5df45905c3c81870a2b3a87f360e237ac755ff34","message":"feat(keyboard): Step E — Cmd+A, Cmd+D no-grouping, smart-paste, P2 page focus (#80)","branch":"claude/feat-keyboard-binding-registry","timestamp":"2026-05-13T01:46:27.961Z"}},"created_at":"2026-05-12T21:27:33.592Z","updated_at":"2026-05-13T01:46:27.964Z","started_at":"2026-05-13T01:41:46.921Z","completed_at":"2026-05-13T01:46:27.964Z","blockedBy":["y41x08m4"],"blocks":[],"children":[]}
{"id":"y41x08m4","parent_id":"ypz5rro1","name":"D — app-menu.ts accelerators read from the registry","description":"Make the macOS menu read its accelerators from the binding registry so the menu and the dispatcher cannot drift.\n\nFiles added:\n- src/main/runtime/binding-accelerator.ts — exports acceleratorFor(id: BindingId): string. Looks up bindingById(id) and converts NormalizedKey → an Electron accelerator string (e.g. 'CmdOrCtrl+W', 'CmdOrCtrl+Shift+Z'). Reuses the same shape acceleratorString() uses in src/shared/bindings.ts (consider whether the helper can live entirely in shared/bindings.ts and binding-accelerator.ts is just the wrapper; either way is fine).\n\nFiles changed:\n- src/main/runtime/app-menu.ts — every menu item that today hard-codes an accelerator string switches to acceleratorFor('<binding-id>'). The click handler invokes mainHandlers['<binding-id>'](ctx) so menu clicks and keystrokes run the same code.\n\nMenu items WITHOUT a binding (About dialog, Setup, etc.) keep their inline click: — they're not bindable anyway.\n\nBindings expected to drive menu items: close-tab (Cmd+W), undo (Cmd+Z), redo (Cmd+Shift+Z), reset-viewport (Cmd+1), select-all (Cmd+A — wired in Step E if not already), group (Cmd+G), ungroup (Cmd+Shift+G), duplicate (Cmd+D). Audit app-menu.ts for the full list.\n\nNo behavior change for users. Verify the menu still works (smoke test or manual verification — click each menu item, confirm the action runs).\n\nReferences:\n- docs/plans/keyboard-binding-registry.md §Menu accelerators, §Step D","priority":1,"completed":true,"result":"Step D merged in PR #79. app-menu.ts Close Tab item now reads accelerator + invokes handler via binding registry. buildBindingContext extracted for non-keystroke callers. Also fixed a Step C regression (missing DRAWING_FEATURE_ENABLED imports) that was breaking typecheck on the feature branch.","metadata":{"commit":{"sha":"98205fa7c1d4983659371f10bf2acd966e931a55","message":"feat(keyboard): Step D — app-menu accelerators read from the registry (#79)","branch":"claude/feat-keyboard-binding-registry","timestamp":"2026-05-13T01:23:04.950Z"}},"created_at":"2026-05-12T21:27:21.363Z","updated_at":"2026-05-13T01:23:04.953Z","started_at":"2026-05-13T01:19:15.046Z","completed_at":"2026-05-13T01:23:04.953Z","blockedBy":["fca2e239"],"blocks":["r8cru8pl"],"children":[]}
{"id":"y46q42dn","parent_id":"ypz5rro1","name":"B — port main's keyboard-shortcuts.ts to the registry","description":"Wire main process to the registry. Replaces watchModifierKeys() and the late-bound wireKeyboardShortcuts() setter.\n\nFiles added/changed:\n- src/main/runtime/binding-handlers.ts — main-side handler map (Record<MainBindingId, (ctx: BindingContext) => void>). Imports concrete runtime functions directly. No late-bound setters; the cycle is broken by ordering this file after runtime-core init.\n- src/main/runtime/binding-dispatcher.ts — exports attachBindingDispatcher(webContents, sourceView). Registers a before-input-event listener that normalizes input → NormalizedKey, builds BindingContext from runtime state, calls dispatchKey, then either invokes mainHandlers[id] or webContents.send('binding-fire', id) for renderer-targeted bindings.\n- src/main/runtime/window-init.ts — replace watchModifierKeys(...) calls with attachBindingDispatcher(...). Delete the wireKeyboardShortcuts(...) injection call (window-init.ts:132).\n- src/main/runtime/page-factory.ts — same swap.\n\nFiles deleted:\n- src/main/runtime/keyboard-shortcuts.ts\n\nAdjacent IPC additions:\n- selectAllEntities — new IPC handler that selects every entity in the active tab. Used by Cmd+A when not typing. (This pairs with Step E's Cmd+A wiring but the IPC needs to exist now so the main handler can call it.)\n\nVariant key handlers (per ADR 0009): shape and brush variant bindings are multi-statement handler bodies that set the active tool and write the tool default. Example:\n  'tool-add-shape-rectangle': () => { setActiveTool({ kind: 'add-shape' }); setToolDefault('add-shape.shapeKind', 'rectangle') }\n\nTests:\n- Existing smoke tests should pass unchanged.\n- Add tests/smoke/keyboard-shortcuts.test.ts — happy path for every binding (Cmd+Z, Cmd+G, v, c, m, Escape exits tool, Cmd+1 reset viewport, etc.). Cmd+A/Cmd+D/paste-smart/page-focus reservation belong to Step E.\n\nSuppression rules implemented in dispatcher (not the table):\n- Default: skip if isTextEditing is true.\n- Bindings with firesWhileTyping: true bypass the typing check: undo, redo, reset-viewport, escape-tool, close-tab.\n- Page focus: skip if pageFocusActive is true unless firesFromPageFocus is true (reset-viewport, escape-tool, escape-page-focus).\n\nReferences:\n- docs/plans/keyboard-binding-registry.md §Main adapter, §Main handler map, §Step B, §Variant key handlers, §Behavior rules\n- docs/adr/0010-main-as-sole-shortcut-dispatch-site.md","priority":1,"completed":true,"result":"Merged PR #77, squash commit 34d694ea9eec0e9d757297c766507c5fa4c99d63","metadata":null,"created_at":"2026-05-12T21:27:00.732Z","updated_at":"2026-05-12T23:30:00.000Z","started_at":"2026-05-12T23:00:00.000Z","completed_at":"2026-05-12T23:30:00.000Z","blockedBy":["ltiqxivt"],"blocks":["fca2e239"],"children":[]}
{"id":"ypz5rro1","parent_id":null,"name":"Keyboard Binding Registry","description":"# Keyboard Binding Registry\n\nBranch: `claude/explore-keyboard-system-N5fjt`. Plan for collapsing Specular's\nkeyboard system into a single binding registry. The registry is the source of\ntruth for every shortcut; main is the sole dispatch site; renderer keydown\nlisteners go away. See [ADR 0010](../adr/0010-main-as-sole-shortcut-dispatch-site.md)\nand [ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md).\n\n## Status\n\n| Step | Status |\n|---|---|\n| A — registry + dispatch + tests, no callers | ⬜ not started |\n| B — port main's `keyboard-shortcuts.ts` to the registry | ⬜ not started |\n| C — delete renderer keydown hooks; port to registry | ⬜ not started |\n| D — `app-menu.ts` accelerators read from the registry | ⬜ not started |\n| E — adjacent: `Cmd+A` select-all, Cmd+D no-grouping, paste-smart, P2 page focus | ⬜ not started |\n\nEach step ends in a working app. Each ships as one PR.\n\n## Context\n\nKeyboard shortcuts today are scattered across:\n\n- `src/main/runtime/keyboard-shortcuts.ts` — 285 lines of `if (input.type === 'keyDown' && input.meta && …)` blocks.\n- Three renderer hooks (`useCanvasGlobalShortcuts`, `useAnnotateToggleShortcut`, `useAnnotationOverlayShortcuts`) that re-handle the same v/c/d keys when focus is in non-canvas views.\n- `src/main/runtime/app-menu.ts` accelerator strings, totally disconnected from the rest.\n- Per-WebContents `handleShortcuts: false` flag, which is the implicit \"this view doesn't fire tool shortcuts\" filter.\n\nEffects:\n\n- \"Where is the `v` shortcut defined?\" has three answers depending on which WebContents has focus.\n- No tests exist on any of the keyboard code.\n- The wiring uses a late-bound setter pattern (`wireKeyboardShortcuts(...)`) to dodge an import cycle.\n- ADR 0005 explicitly flagged keyboard shortcuts as the natural follow-up to the unified `Tool` concept. ADR 0009 §93 flagged variant-key bindings (R/E/D, etc.) as a follow-up to tool-defaults.\n\nThis plan finishes that follow-up.\n\n## Goals\n\n1. **One source of truth for every shortcut.** The Bindings UI and the menu accelerators and the dispatcher all read the same table.\n2. **One dispatch site.** Main's `before-input-event`. No renderer `keydown` listeners for shortcuts. ([ADR 0010](../adr/0010-main-as-sole-shortcut-dispatch-site.md))\n3. **Coverage parity.** Every `Tool['kind']` has a default key (TypeScript exhaustiveness enforces it). Adding a new tool without a key is a build error.\n4. **Predictable suppression.** Plain letters never fire while typing. Modifier shortcuts opt in to firing while typing. Same shape applies to page focus. ([ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md))\n5. **Pure-function test surface.** Dispatch is unit-testable without spawning Electron.\n\n## Non-goals (v1)\n\n- Bindings settings pane.\n- User overrides (no `userData/bindings.json`).\n- `?` cheatsheet overlay.\n- Toolbar tooltips. (Tooltips land in a later PR and become the source of truth for discoverability.)\n\n## Architecture\n\n### Modules\n\n```\nsrc/shared/bindings.ts            // pure data + types + dispatchKey()\nsrc/main/runtime/binding-handlers.ts   // Record<BindingId, (ctx) => void>\nsrc/main/runtime/binding-dispatcher.ts // watchModifierKeys replacement; per-WebContents adapter\nsrc/renderer/{view}/binding-handlers.ts // small per-renderer handler maps (only for renderer-targeted bindings)\nsrc/renderer/{view}/useRendererBindingHandlers.ts // tiny hook that listens for 'binding-fire' IPC\n```\n\nDeleted:\n- `keyboard-shortcuts.ts` (replaced by `binding-dispatcher.ts` + `binding-handlers.ts`)\n- `useAnnotateToggleShortcut.ts`\n- `useAnnotationOverlayShortcuts.ts`\n- v/c/d/escape/delete branches of `useCanvasGlobalShortcuts.ts` (clipboard remains, split into `useCanvasClipboard.ts`)\n- `wireKeyboardShortcuts(...)` late-bound setter pattern\n- `handleShortcuts: false` per-view flag\n\n### Types\n\n```ts\n// src/shared/bindings.ts\n\nexport type KeyboardSourceView =\n  | 'aboveView' | 'canvasBg'\n  | 'toolbar' | 'leftSidebar' | 'rightDetailsPanel'\n  | 'devtoolsHeader' | 'devtoolsResizeHandle'\n  | 'page'\n\nexport type NormalizedKey = {\n  key: string      // lowercased, e.g. 'v', 'escape', 'arrowleft'\n  cmd: boolean     // CmdOrCtrl\n  alt: boolean\n  shift: boolean\n}\n\nexport type BindingId =\n  | 'tool-select' | 'tool-add-page' | 'tool-add-text-plain' | 'tool-add-text-sticky'\n  | 'tool-add-shape-rectangle' | 'tool-add-shape-ellipse' | 'tool-add-shape-diamond'\n  | 'tool-comment' | 'tool-draw-pen' | 'tool-draw-highlight' | 'tool-inspect'\n  | 'undo' | 'redo' | 'reset-viewport' | 'group' | 'ungroup'\n  | 'select-all' | 'duplicate' | 'delete-selection'\n  | 'nav-left' | 'nav-right' | 'nav-up' | 'nav-down'\n  | 'escape-tool' | 'escape-page-focus'\n  | 'close-tab'\n  | 'annotation-close-thread' | 'annotation-clear-draft'\n\nexport type BindingTarget = 'main' | KeyboardSourceView\n\nexport type BindingContext = {\n  activeTool: Tool\n  isTextEditing: boolean\n  arrowNavigationLocked: boolean\n  hasKeyboardTargetPage: boolean\n  pageFocusActive: boolean\n  canUndo: boolean\n  canRedo: boolean\n  selectionEmpty: boolean\n  sourceView: KeyboardSourceView\n  viewMode: 'canvas' | 'browser'\n}\n\nexport type Binding = {\n  id: BindingId\n  defaultKey: NormalizedKey\n  scope: KeyboardSourceView[]        // which source views can fire this binding\n  target: BindingTarget              // where the handler runs\n  firesWhileTyping?: boolean         // default false\n  firesFromPageFocus?: boolean       // default false\n  when?: (ctx: BindingContext) => boolean\n  label: string                      // for future Bindings pane + tooltips\n}\n```\n\n### Dispatch\n\n```ts\n// src/shared/bindings.ts\n\nexport function dispatchKey(\n  table: readonly Binding[],\n  key: NormalizedKey,\n  ctx: BindingContext,\n): BindingId | null\n```\n\nPure function. Returns the first binding whose `defaultKey` matches the input\nkey and whose `scope`/`when`/`firesWhileTyping`/`firesFromPageFocus` predicates\nall hold.\n\nMatch order: table order. No conflict detection needed because each binding's\nscope is explicit; if two bindings have the same key + scope + modifiers, that's\na typo, caught by a unit test that asserts uniqueness.\n\n### Main adapter\n\n```ts\n// src/main/runtime/binding-dispatcher.ts\n\nexport function attachBindingDispatcher(\n  webContents: WebContents,\n  sourceView: KeyboardSourceView,\n): void\n```\n\nRegisters a `before-input-event` listener. On keydown:\n\n1. Normalize the Electron `Input` → `NormalizedKey`.\n2. Build `BindingContext` from runtime state (active tool, text-editing flag, …).\n3. Call `dispatchKey(BINDINGS, key, { ...ctx, sourceView })`.\n4. If a `BindingId` came back:\n   - `event.preventDefault()`.\n   - If `binding.target === 'main'`: invoke `mainHandlers[id](ctx)`.\n   - Else: `viewFor(binding.target).webContents.send('binding-fire', id)`.\n\nThis replaces `watchModifierKeys`. The `sourceView` argument replaces the\n`handleShortcuts: false` flag.\n\n### Main handler map\n\n```ts\n// src/main/runtime/binding-handlers.ts\n\nexport const mainHandlers: Record<MainBindingId, (ctx: BindingContext) => void>\n```\n\nImports concrete runtime functions directly. No late-bound setters. The cycle\nthat motivated `wireKeyboardShortcuts(...)` is broken by placing this file at\na layer that runs *after* `runtime-core` is initialized.\n\n### Renderer handler maps\n\n```ts\n// src/renderer/above-view/binding-handlers.ts\n\nexport const aboveViewBindingHandlers: Record<AboveViewBindingId, () => void>\n```\n\nRegistered via a small hook on mount:\n\n```ts\nuseRendererBindingHandlers(aboveViewBindingHandlers)\n```\n\nThe hook listens for the `binding-fire` IPC and invokes the matching handler.\nTwo bindings use this path today: `annotation-close-thread` and\n`annotation-clear-draft`, because their state (`openThreadId`,\n`pendingAnnotation`) is renderer-local React `useState`.\n\n### Menu accelerators\n\n```ts\n// src/main/runtime/app-menu.ts\n\nimport { acceleratorFor } from './binding-accelerator'\n\n{ label: 'Close Tab', accelerator: acceleratorFor('close-tab'), click: () => mainHandlers['close-tab']({}) }\n```\n\n`acceleratorFor(id)` returns the `CmdOrCtrl+W`-style string derived from\n`bindingById(id).defaultKey`. The menu and the dispatcher cannot drift.\n\nMenu items without a binding (About dialog, Setup) keep their inline `click:`\n— they're not bindable anyway.\n\n## The binding table\n\n| Id | Default key | Scope | Target | While typing? | From page focus? |\n|---|---|---|---|---|---|\n| `tool-select` | `v` | canvas-region¹ | main | — | — |\n| `tool-add-page` | `p` | canvas-region | main | — | — |\n| `tool-add-text-plain` | `t` | canvas-region | main | — | — |\n| `tool-add-text-sticky` | `s` | canvas-region | main | — | — |\n| `tool-add-shape-rectangle` | `r` | canvas-region | main | — | — |\n| `tool-add-shape-ellipse` | `o` | canvas-region | main | — | — |\n| `tool-add-shape-diamond` | `Shift+R` | canvas-region | main | — | — |\n| `tool-comment` | `c` | canvas-region | main | — | — |\n| `tool-draw-pen` | `m` | canvas-region | main | — | — |\n| `tool-draw-highlight` | `Shift+M` | canvas-region | main | — | — |\n| `tool-inspect` | `i` | canvas-region | main | — | — |\n| `escape-tool` | `Escape` | all | main | yes (commits text edit instead) | yes |\n| `escape-page-focus` | `Escape` | `page` | main | — | yes |\n| `undo` | `Cmd+Z` | all | main | **yes** | — |\n| `redo` | `Cmd+Shift+Z` | all | main | **yes** | — |\n| `reset-viewport` | `Cmd+1` | all | main | **yes** | **yes** |\n| `group` | `Cmd+G` | canvas-region | main | — | — |\n| `ungroup` | `Cmd+Shift+G` | canvas-region | main | — | — |\n| `select-all` | `Cmd+A` | canvas-region | main | — | — |\n| `duplicate` | `Cmd+D` | canvas-region | main | — | — |\n| `delete-selection` | `Delete` / `Backspace` | canvas-region | main | — | — |\n| `nav-left` | `ArrowLeft` | canvas-region | main | — | — |\n| `nav-right` | `ArrowRight` | canvas-region | main | — | — |\n| `nav-up` | `ArrowUp` | canvas-region | main | — | — |\n| `nav-down` | `ArrowDown` | canvas-region | main | — | — |\n| `close-tab` | `Cmd+W` | all | main | yes | — |\n| `annotation-close-thread` | `Escape` | `aboveView` | `aboveView` | — | — |\n| `annotation-clear-draft` | `Escape` | `aboveView` | `aboveView` | — | — |\n\n¹ `canvas-region` is a convenience for `['aboveView', 'canvasBg', 'toolbar', 'rightDetailsPanel']` — the four views where canvas-style shortcuts should fire. Constant defined in `src/shared/bindings.ts`.\n\n### `add-document` intentionally unbound\n\nHas no default key. Users add documents via the toolbar's \"Add text ▾\"\ndropdown. Bindable later via the (future) Bindings pane.\n\n### `Escape` resolution order\n\nThree bindings share `Escape`. Match order is:\n\n1. `annotation-close-thread` / `annotation-clear-draft` (scope: `aboveView`, only when active state predicate holds — the `when` checks for `openThreadId` / `pendingAnnotation`).\n2. `escape-page-focus` (scope: `page` only, only when page focus is active).\n3. `escape-tool` (all scopes, fires whenever `activeTool.kind !== 'select'`).\n\nEach binding's `when` predicate ensures only one fires per keystroke; if none of\nthem match, Escape falls through to native handling.\n\n### Variant key handlers\n\nPer [ADR 0009](../adr/0009-tool-variants-in-popup-state.md), shape and brush\nvariants live in tool defaults. The keyboard handler does **both**:\n\n```ts\n'tool-add-shape-rectangle': () => {\n  setActiveTool({ kind: 'add-shape' })\n  setToolDefault('add-shape.shapeKind', 'rectangle')\n}\n'tool-draw-highlight': () => {\n  setActiveTool({ kind: 'draw' })\n  setToolDefault('draw.brushType', 'highlight')\n}\n```\n\nNo compound-action framework needed — it's just a multi-statement handler body.\n\n## Behavior rules\n\n### No tool-toggle on second keypress\n\nFigJam reference: pressing the same tool key twice does nothing (the tool stays\nactive). Escape is the only way back to `select`. This removes the existing\ntoggle behavior in `useAnnotateToggleShortcut.ts`.\n\n### Suppression while typing\n\nDefault: bindings do not fire while `isTextEditing` is true. Opt-in via\n`firesWhileTyping: true`. Four bindings opt in:\n\n- `undo`, `redo` — global Yjs undo spans text + canvas edits.\n- `reset-viewport` — hand-of-god. Viewport reset doesn't mutate the document.\n- `escape-tool` — Escape in a text input commits the edit; the text-edit\n  context check inside the handler prevents double-firing as a tool reset.\n\n`Cmd+A` is contextual: when `isTextEditing`, the native \"select all text\" wins\nbecause the binding's `firesWhileTyping` is `false`. When not typing, our\n`select-all` handler invokes `selectAllEntities()`.\n\n### Page focus (P2 policy)\n\nPer [ADR 0011](../adr/0011-page-focus-respects-native-shortcuts.md), when a\npage has keyboard focus, the page wins almost all keystrokes. Specular reserves\nonly:\n\n- `escape-page-focus` — exit page focus back to canvas.\n- `reset-viewport` — hand-of-god.\n\nCmd+Z, Cmd+G, Cmd+W, arrows, and tool keys all fall through to the page\nnatively. To use canvas undo while page-focused, the user presses Escape first.\n\nThis is a behavior change from today. Changelog entry required.\n\n## Clipboard split\n\nClipboard handling is **not** in the registry. `Cmd+C` / `Cmd+V` / `Cmd+X` arrive\nas DOM `ClipboardEvent`s, not as keystrokes — and `event.clipboardData` /\n`window.getSelection()` only exist renderer-side.\n\nThe new `src/renderer/canvas-bg/useCanvasClipboard.ts` (extracted from\n`useCanvasGlobalShortcuts.ts`) owns this. The hijack rule:\n\n- If `event.target` is a typing target, native wins.\n- If `window.getSelection().toString()` is non-empty, native wins.\n- Else, the canvas hijacks.\n\nOn hijack:\n\n- **Copy** with entity selection → entity JSON to clipboard.\n- **Cut** with entity selection → copy + delete.\n- **Paste** smart-resolution order (`pasteFromClipboard()` in main):\n  1. Our entity-shaped JSON → paste entities.\n  2. Text that `looksLikeUrl()` matches → create a page (reuses existing URL paster).\n  3. Image data → create a file entity.\n  4. Plain text → create a sticky note.\n  5. Otherwise → no-op.\n\nActive page intercepts nothing — when page focus is held, the page receives\nclipboard events natively. The renderer hook installs no listeners on the page.\n\n## Duplicate cleanup\n\n`Cmd+D` reuses `duplicatePageFromSource` / `duplicateEntity`. Side cleanup:\n\n- Default `skipGrouping: true` across all three call sites in\n  `register-canvas-entity-ipc.ts` and `register-right-details-panel-ipc.ts`.\n- Remove the `mode: 'duplicate'` grouping codepath through `addPageFromSource`.\n\nThis is a behavior change for the existing context-menu and right-details-panel\nduplicate buttons — they no longer auto-create row groups. Changelog entry\nrequired.\n\n## Build order\n\n### Step A — registry + dispatch + tests (no callers)\n\nFiles added:\n- `src/shared/bindings.ts` — types, the table, `dispatchKey()`, `normalizeElectronInput()`, `acceleratorString()`.\n- `tests/unit/bindings.test.ts` — table uniqueness, dispatch for every binding × source view × modifier combination, scope and predicate filtering.\n\nFiles unchanged. App behavior unchanged. Pure addition. Zero risk.\n\n### Step B — port main\n\nFiles added/changed:\n- `src/main/runtime/binding-handlers.ts` — main-side handler map.\n- `src/main/runtime/binding-dispatcher.ts` — `attachBindingDispatcher(wc, sourceView)`.\n- `src/main/runtime/window-init.ts` — replace `watchModifierKeys(...)` calls with `attachBindingDispatcher(...)`.\n- `src/main/runtime/page-factory.ts` — same swap.\n- `src/main/ipc/register-canvas-ipc.ts` — `setTextEditingActive` import path stays.\n\nFiles deleted:\n- `src/main/runtime/keyboard-shortcuts.ts`.\n\nThe `wireKeyboardShortcuts(...)` injection in `window-init.ts:132` is deleted;\n`binding-handlers.ts` imports its dependencies directly.\n\nAdjacent IPC additions:\n- `selectAllEntities` — new IPC handler that selects every entity in the active tab. Used by `Cmd+A` when not typing.\n\nExisting smoke tests should pass unchanged. Add:\n- `tests/smoke/keyboard-shortcuts.test.ts` — happy path for every binding.\n\n### Step C — delete renderer hooks\n\nFiles deleted:\n- `src/renderer/shared/hooks/useAnnotateToggleShortcut.ts`.\n- `src/renderer/shared/hooks/useAnnotationOverlayShortcuts.ts`.\n- v/c/d/escape/delete branches removed from `useCanvasGlobalShortcuts.ts` (it becomes empty for keyboard; remove the hook).\n\nFiles added/changed:\n- `src/renderer/canvas-bg/useCanvasClipboard.ts` — clipboard events only.\n- `src/renderer/above-view/binding-handlers.ts` — `annotation-close-thread`, `annotation-clear-draft`.\n- `src/renderer/shared/hooks/useRendererBindingHandlers.ts` — generic IPC listener.\n\nRenderer App.tsx files updated to use the new hooks.\n\n### Step D — port menu accelerators\n\nFiles changed:\n- `src/main/runtime/app-menu.ts` — `accelerator: acceleratorFor('close-tab')` etc.\n- `src/main/runtime/binding-accelerator.ts` — `acceleratorFor(id)` helper.\n\nThe menu cannot drift from the dispatcher; they read the same table.\n\n### Step E — adjacent behavior changes\n\nLands as part of Step B/C or split into its own PR. Each is small but\nuser-visible:\n\n- `select-all` action + Cmd+A wiring.\n- `duplicate` action: reuse helpers, drop auto-grouping in all paths.\n- Smart-paste resolution.\n- P2 page-focus enforcement.\n\nEach gets a changelog entry. Alt-drag-to-duplicate (pointer-router work) is\n**adjacent** to this plan and should ship in the same release for narrative\ncoherence, but is tracked separately because it touches the router not the\nkeyboard.\n\n## Tests\n\n### Unit\n\n- `bindings.test.ts` — the table is well-formed (every `Tool['kind']` has a binding; no two bindings share `(key, scope, modifiers)`).\n- `bindings.test.ts` — `dispatchKey` returns the right id for every binding × source-view × context combination.\n- `bindings.test.ts` — `acceleratorString` round-trips for every modifier combination.\n- `bindings.test.ts` — `firesWhileTyping` and `firesFromPageFocus` defaults are respected.\n\n### Smoke\n\n- `keyboard-shortcuts.test.ts` — Cmd+Z, Cmd+G, v, c, m, paste-URL-creates-page, paste-text-creates-sticky, Cmd+A select-all, Cmd+D duplicate-no-grouping, Escape exits page focus, Escape exits tool, Cmd+1 from page focus resets viewport.\n\n### Regression\n\n- Existing tests for tool switching, undo/redo, grouping, paste — should all pass unchanged after the port. Only the wiring changed, not the actions.\n\n## Out of scope\n\n- Bindings settings pane (UI).\n- User-override storage.\n- Toolbar tooltips (separate PR; reads from the same registry).\n- `?` cheatsheet overlay (later).\n- Empty-canvas onboarding hints.\n- Alt-drag-to-duplicate (pointer-router work; adjacent).\n- Promoting `pendingAnnotation` / `openThreadId` to main runtime state (would\n  let those bindings be `target: 'main'`; not worth the scope creep here).\n\n## Migration notes\n\n- `wireKeyboardShortcuts(...)` deleted — search for it; one call site in `window-init.ts`.\n- `watchModifierKeys(...)` deleted — every call site swaps to `attachBindingDispatcher(wc, sourceView)`.\n- `handleShortcuts: false` flag deleted — replaced by per-binding `scope`.\n- `setTextEditingActive(...)` stays — it's the same flag, same IPC, same logic.\n- `useAnnotateToggleShortcut` / `useAnnotationOverlayShortcuts` deleted; consumers updated.\n- Tool-toggle behavior removed: pressing `c` while in comment tool no longer goes back to select. Changelog.\n- Duplicate no longer auto-groups. Changelog.\n- Page focus no longer captures Cmd+Z and friends. Changelog.\n\n## Future work\n\n- **Bindings pane in Settings.** Reads `listBindings()`. β1 (read-only) for v1\n  of the pane; β2 (rebinding + `userData/bindings.json`) later.\n- **Toolbar tooltips.** Reads `binding.label` + `acceleratorString(binding.key)`\n  for each toolbar button. Will be the source of truth for discoverability.\n- **`?` overlay.** Modal cheatsheet listing all bindings. One-day add once the\n  registry exists.\n- **Per-binding context labels.** \"Group entities (when entities selected)\" —\n  the `when` predicate gets a human-readable explanation for the Bindings pane.\n","priority":1,"completed":false,"result":null,"metadata":null,"created_at":"2026-05-12T21:26:32.058Z","updated_at":"2026-05-12T21:26:32.058Z","started_at":null,"completed_at":null,"blockedBy":[],"blocks":[],"children":["ltiqxivt","y46q42dn","fca2e239","y41x08m4","r8cru8pl"]}
